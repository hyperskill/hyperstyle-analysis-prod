name: Build

on: [ push, pull_request ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: 'Checkout'
        uses: actions/checkout@v2

      - name: 'Build Docker image'
        run: docker build -t hyperstyle-analysis-prod .

      - name: 'Run tests'
        run: docker run hyperstyle-analysis-prod poetry pytest -vv
